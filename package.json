{
  "name": "flixhome-asystent",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @repo/shared build && pnpm --filter web build",
    "dev:web": "pnpm --filter @repo/shared build && pnpm --filter web dev",
    "dev:api": "cd apps/api && uv run uvicorn main:app --reload",
    "dev": "pnpm run dev:web"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.14.2",
  "devDependencies": {
    "next": "15.5.13"
  }
}
