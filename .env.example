# Skopiuj do .env i uzupełnij. Nie commituj .env (jest w .gitignore).
# Opis zmiennych: docs/runbook.md

# --- Web (frontend) — używane przy pnpm --filter web dev ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# --- Backend (API) czyta .env z apps/api/ — tam utwórz apps/api/.env ---
# Zobacz apps/api/.env.example
