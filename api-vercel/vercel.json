{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cp -r ../apps . && pip install -r requirements.txt",
  "installCommand": "pip install -r requirements.txt",
  "functions": {
    "api/backend.py": {
      "maxDuration": 30
    }
  }
}
